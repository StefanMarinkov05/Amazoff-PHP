; docker-php-ext-enable pcov already wrote extension=pcov.so; this file only
; carries the settings. Scoped to the app source so vendor/ and the test
; files themselves don't inflate or dilute the report.
pcov.enabled = 1
pcov.directory = /var/www/html
pcov.exclude = "~vendor|tests|bootstrap/cache|storage~"
